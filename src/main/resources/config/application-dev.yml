#»’÷æ≈‰÷√
logging:
  config: classpath:config/log4j2-dev.xml
  level: info
#mybatis ≈‰÷√
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.app.admin.server.bean
#mysql≈‰÷√
spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://127.0.0.1:3306/app-admin?useSSL=true&useUnicode=true&characterEncoding=UTF-8
    driver-class-name: com.mysql.jdbc.Driver
    tomcat:
      max-wait: 10000
      max-idle: 10
      min-idle: 5
      initial-size: 5
      validation-query: select 1
